# SQLite migrations (archived)

These migrations applied to the pre–Phase 6 SQLite database (`dev.db`). They are kept for history only.

PostgreSQL uses a fresh baseline in `prisma/migrations/`. Existing `dev.db` data is **not** auto-migrated to Postgres.
